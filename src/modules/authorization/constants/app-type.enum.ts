/**
 * Tipo de aplicación para permisos
 *
 * - FRONTEND: Rutas de la aplicación web (ej: /admin/users)
 * - BACKEND: Endpoints de la API (ej: /api/users)
 */
export enum AppType {
  FRONTEND = 'frontend',
  BACKEND = 'backend',
}
